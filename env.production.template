# ===========================================
# AI Project - 生产环境配置模板
# ===========================================

# ===========================================
# NextAuth 配置
# ===========================================
# 你的 Vercel 应用 URL (部署后需要更新)
NEXTAUTH_URL=https://your-app-name.vercel.app

# NextAuth 密钥 (已生成的安全密钥)
NEXTAUTH_SECRET=ihHd+ZkvByDA0TTtowiJU/Gg3bF1WESoYrVA+NhMmY4=

# ===========================================
# 数据库配置
# ===========================================
# 推荐使用 Vercel Postgres (免费)
# 在 Vercel Dashboard -> Storage -> Create Database -> Postgres
# 创建后会自动生成 DATABASE_URL
DATABASE_URL=postgresql://username:password@host:port/database

# 用于 Prisma 连接池 (通常与 DATABASE_URL 相同)
DIRECT_URL=postgresql://username:password@host:port/database

# ===========================================
# OAuth 提供商配置 (可选)
# ===========================================
# Google OAuth (可选)
# 在 https://console.developers.google.com/ 创建 OAuth 应用
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# GitHub OAuth (可选)
# 在 https://github.com/settings/developers 创建 OAuth 应用
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# ===========================================
# 其他配置
# ===========================================
NODE_ENV=production

# ===========================================
# 使用说明
# ===========================================
# 1. 复制此文件内容到 Vercel 控制台的环境变量中
# 2. 更新 NEXTAUTH_URL 为你的实际 Vercel 应用 URL
# 3. 配置数据库连接 (推荐使用 Vercel Postgres)
# 4. 如需 OAuth 登录，配置相应的客户端 ID 和密钥
# 5. 所有环境变量都设置为 "Production" 环境
