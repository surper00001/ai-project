version: '3.8'

services:
  # 使用服务器上的PostgreSQL数据库
  # 不需要在Docker中创建数据库容器

  # Next.js应用
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-project-app
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://postgres:Tt149212@39.96.218.145:5432/postgres
      - DIRECT_URL=postgresql://postgres:Tt149212@39.96.218.145:5432/postgres
      - NEXTAUTH_SECRET=ihHd+ZkvByDA0TTtowiJU/Gg3bF1WESoYrVA+NhMmY4=
      - NEXTAUTH_URL=http://39.96.218.145
      - QWEN_API_KEY=sk-30a8e6a72d6a4bda97f0ae9ca7d02fa3
      - QWEN_API_URL=https://dashscope.aliyuncs.com/api/v1/services/aigc/text-generation/generation
    network_mode: "host"
    volumes:
      - ./uploads:/app/uploads

  # Nginx反向代理
  nginx:
    image: nginx:alpine
    container_name: ai-project-nginx
    restart: unless-stopped
    network_mode: "host"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./ssl:/etc/nginx/ssl
      - nginx_logs:/var/log/nginx
    depends_on:
      - app
    environment:
      - NGINX_HOST=39.96.218.145
      - NGINX_PORT=80

volumes:
  nginx_logs:
